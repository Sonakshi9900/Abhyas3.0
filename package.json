{
  "name": "abhyastre-root",
  "version": "1.0.0",
  "description": "AbhyasTRE BPSC & D.El.Ed Prep Platform Root Runner",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "build": "cd client && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../dist', { recursive: true })\"",
    "seed": "npm run seed --prefix server",
    "generate-pdfs": "node server/scripts/generatePDFs.js",
    "test": "npm test --prefix server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
