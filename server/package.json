{
  "name": "abhyastre-server",
  "version": "1.0.0",
  "description": "Backend API for AbhyasTRE BPSC Preparation Platform",
  "main": "server.js",
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/seedData.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "jest": "^30.5.1",
    "jsonwebtoken": "^9.0.3"
  }
}
